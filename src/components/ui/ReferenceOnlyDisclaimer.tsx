import React from "react";

export const ReferenceOnlyDisclaimer: React.FC = () => {
    return (
        <p className="text-xs text-muted-foreground mt-2">
            Reference-only: derived from your entries. Always verify before sending.
        </p>
    );
};
